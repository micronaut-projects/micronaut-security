The default posture for `script-src` is to add a cryptographically random nonce to `script-src` for every response and enable

The `script-src` directive is populated via api:security.csp.ScriptSrcGenerator[] with its default implementation of api:security.csp.DefaultScriptSrcGenerator[].
