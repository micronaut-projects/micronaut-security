The default posture for `script-src` is to add a cryptographically random nonce to `script-src` for every response and enable https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Content-Security-Policy/script-src#strict-dynamic[strict-dynamic].
