introduction:
  title: Introduction
whatsNew: What's New
authenticationProviders: Authentication Providers
securityRule:
  title: Security Rules
  ipPattern: IP Pattern Rule
  secured: Secured Annotation
  interceptUrlMap: Intercept URL Map
  builtInEndpointsAccess: Built-In Endpoints Security
authenticationStrategies:
  title: Authentication Strategies
  basicAuth: Basic Auth
  session: Session Authentication
  jwt:
    title: JSON Web Token
    reader:
      title: Reading JWT Token
      bearerToken: Bearer Token Reader
      cookieToken: Cookie Token Reader
    jwtGenerator:
      title: JWT Token Generation
      jwtGeneratorSignature: JWT Signature
      jwtGeneratorEncryption: Encrypted JWTs
    jwtValidation:
      title: JWT Token Validation
      jwks: Validation with remote JWKS
    claimsGeneration: Claims Generation
    render:
      title: Token Render
  ldap:
    title: LDAP Authentication
    configuration: Configuration
    extending: Extending Default Behavior
rejection: Rejection Handling
tokenPropagation: Token Propagation
endpoints:
  title: Built-In Security Controllers
  login: Login Controller
  logout: Logout Controller
  refresh: Refresh Controller
  keys: Keys Controller
retrievingAuthenticatedUser:
  title: Retrieve the authenticated user
  securityService: User outside of a controller
securityEvents: Security Events
oauth:
  title: OAuth 2.0
  installation:
    title: Installation
  openid:
    title: Open ID Connect
  service-discovery:
    title: Service Discovery
    service-discovery-okta: Service Discovery with OKTA
    service-discovery-cognito: Service Discovery with AWS Cognito
    service-discovery-auth0: Service Discovery with Auth0
    service-discovery-keycloak: Service Discovery with Keycloak
  oauth-endpoints:
    title: Endpoints
    authorization:
      title: Authorization Endpoint
      state: State
      idTokenHint: IDToken Hint
      loginHint: Login Hint
      nonce: Nonce
    token: Token Endpoint
    endsession:
      title: End Session Endpoint
      logoutController: Logout Controller
      logouturl: Logout URL
  providerSpecificBeans:
    title: Provider Specific Beans
  flows:
    title: Flows
    authorization-code-grant-type:
      title: Authorization Code Grant Flow
      authorization-code-controller: Authorization Code Controller
      id-token-validator: ID Token Validator
      failure-handler: Failure Handler
      successful-handler: Successful ID Token Handler
      cookie-successful-handler: Cookie Successful ID Token Handler
    password-grant-type: Password Grant Flow
