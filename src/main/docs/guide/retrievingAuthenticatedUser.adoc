Often, you may want to retrieve the authenticated user.

You can bind `java.security.Principal` as a method parameter in a controller.

snippet::io.micronaut.security.docs.principalparam.UserController[tags="imports,clazz"]

If you need more detail, you can bind api:security.authentication.Authentication[] as a method parameter in a controller.

snippet::io.micronaut.security.docs.authenticationparam.UserController[tags="imports,clazz"]

