plugins {
    id 'java-library'
    id "io.micronaut.build.internal.security-tests"
}

dependencies {
    annotationProcessor(platform(mn.micronaut.bom))
    annotationProcessor(mn.micronaut.inject.java)
    implementation(platform(mn.micronaut.bom))
    implementation(project(":security-jwt"))
    implementation(project(":security-oauth2"))
    implementation(project(":test-suite-utils"))
}

java {
    sourceCompatibility = JavaVersion.toVersion("17")
    targetCompatibility = JavaVersion.toVersion("17")
}
