plugins {
    id("io.micronaut.build.internal.bom")
}
dependencies {
    constraints {
        api("com.nimbusds:nimbus-jose-jwt:$nimbusJoseJwtVersion")
    }
}
micronautBuild {
    binaryCompatibility {
        enabled = false
    }
}
